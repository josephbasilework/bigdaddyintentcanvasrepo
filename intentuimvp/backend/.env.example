# ==============================================================================
# IntentUI Backend Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your values.
# The .env file is loaded automatically on application startup.
#
# Required vs Optional:
# - REQUIRED: Application will not start without this variable
# - OPTIONAL: Has a default value that can be overridden
# ==============================================================================

# ------------------------------------------------------------------------------
# Pydantic AI Gateway Configuration
# ------------------------------------------------------------------------------
# REQUIRED: Pydantic AI Gateway API key for all LLM inference
# All LLM calls must go through the Gateway (no direct provider SDK imports).
# Get your key at: https://gateway.pydantic.dev
PYDANTIC_GATEWAY_API_KEY=your-gateway-api-key-here

# OPTIONAL: Base URL for the Pydantic AI Gateway proxy
# Default: https://gateway.pydantic.dev/proxy/openai/
# Format: Full URL with trailing slash
PYDANTIC_GATEWAY_BASE_URL=https://gateway.pydantic.dev/proxy/openai/

# ------------------------------------------------------------------------------
# xAI Configuration (Optional)
# ------------------------------------------------------------------------------
# OPTIONAL: xAI API key for fallback models (e.g., Grok)
# Leave empty to use only Pydantic Gateway
# Get your key at: https://x.ai
XAI_API_KEY=

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
# OPTIONAL: Application name
# Default: IntentUI Backend
APP_NAME=IntentUI Backend

# OPTIONAL: Application version
# Default: 0.1.0
APP_VERSION=0.1.0

# OPTIONAL: Deployment environment
# Allowed values: dev, staging, prod
# Default: dev
ENVIRONMENT=dev

# OPTIONAL: Enable debug mode (disables API docs in production)
# Allowed values: true, false
# Default: false
DEBUG=false

# OPTIONAL: Log level
# Allowed values: debug, info, warning, error, critical
# Default: info
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# API Configuration
# ------------------------------------------------------------------------------
# OPTIONAL: API route prefix
# Default: /api/v1
API_PREFIX=/api/v1

# OPTIONAL: CORS allowed origins (JSON array format)
# Add frontend URLs that need API access
# Format: ["http://localhost:3000","http://localhost:8000","https://example.com"]
# Default: ["http://localhost:3000","http://localhost:8000"]
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# OPTIONAL: Database connection URL
#
# SQLite (default, good for development):
#   DATABASE_URL=sqlite:///./intentui.db
#
# PostgreSQL (recommended for production):
#   DATABASE_URL=postgresql://user:password@localhost:5432/dbname
#
# Format: [driver]://[user]:[password]@[host]:[port]/[database]
# Default: sqlite:///./intentui.db
DATABASE_URL=sqlite:///./intentui.db

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# OPTIONAL: Redis URL for job queue (ARQ worker)
# Format: redis://[host]:[port]/[db]
# Default: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# Backup Configuration
# ------------------------------------------------------------------------------
# OPTIONAL: Fernet encryption key for backup data
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Default: empty (backups disabled if not set)
BACKUP_ENCRYPTION_KEY=

# OPTIONAL: Number of days to retain backups
# Default: 30
BACKUP_RETENTION_DAYS=30

# OPTIONAL: Hour to run daily backup (0-23)
# Default: 2 (2 AM)
BACKUP_SCHEDULE_HOUR=2

# OPTIONAL: Enable automatic daily backups
# Allowed values: true, false
# Default: true
BACKUP_ENABLED=true
