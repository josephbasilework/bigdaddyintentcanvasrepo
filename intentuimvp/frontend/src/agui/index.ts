/**
 * AG-UI Protocol: Agent-Gateway UI Communication Protocol
 *
 * This module provides types, protocol definitions, and a WebSocket client
 * for communication between agents (backend) and the UI (frontend).
 *
 * @module agui
 */

// Protocol types and definitions
export * from './protocol';

// WebSocket client
export * from './client';
